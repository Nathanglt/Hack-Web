{% extends 'base.html.twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css">
{% endblock %}

{% block title %}
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
	{% for h in leshackathons %}

			<div class="card" style="width: 18rem;">
				<img src={{h.image}} class="card-img-top">
				<div class="card-body">
					<h5 class="card-title">{{h.titre}}</h5>
					<i class="fa-solid fa-heart"></i>

					{# <a href="{{path('serie',{'id':uneSerie.id})}}" class="btn btn-primary">Détails</a> #}
				</div>
			{% endfor %}
		</div>
	{% endblock %}
