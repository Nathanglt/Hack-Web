{% extends 'base.html.twig' %}

{% block javascripts %}
	<script src="https://kit.fontawesome.com/f84e5ee0a5.js" crossorigin="anonymous"></script>
{% endblock %}

{% block title %}{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	
	<div class="example-wrapper">
		{% for h in leshackathons %}
			<div class="card">
				<img src={{h.image}} class="card-img-top">
				<div class="card-body">
					<h5 class="card-title">{{h.theme}}</h5>
					<a href="{{path('unHackathon',{'id':h.idhackathon})}}" class="btn btn-primary">
						<i class="fa-solid fa-circle-info"></i>
						Détails</a>
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<a href="{{path('favori',{'id':h.idhackathon})}}" postion="right">
							<i class="fa-solid fa-heart"></i>Ajouter en favoris</a>
					{% endif %}

				</div>
			{% endfor %}
		</div>
	{% endblock %}
